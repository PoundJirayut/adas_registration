# Copy ไฟล์นี้เป็น .env แล้วกรอกค่า

# Google Sheet ID (ส่วนที่อยู่ใน URL ระหว่าง /d/ และ /edit)
SHEET_ID=19TspRNs1fkeY89CP-lJW8sdXaTCSGUeD8wh8FPpKoww

# Google Apps Script Web App URL (ได้มาหลัง Deploy)
SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Port ของ server (Railway กำหนดอัตโนมัติ ไม่ต้องตั้งค่าถ้า deploy บน Railway)
PORT=3000

# URL สาธารณะของ server — Railway จะให้ URL มาหลัง deploy
# เปลี่ยนเป็น https://your-app.up.railway.app หลัง deploy
BASE_URL=http://localhost:3000
